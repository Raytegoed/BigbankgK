/**
 * all links are defined globally in this file
 * change rootURL to the right url when deploying on the server
 */
const rootURL = "/"
const tradeButtonLink = rootURL+"PlaceOrder.html"
const loginPageURL = rootURL+"index.html";